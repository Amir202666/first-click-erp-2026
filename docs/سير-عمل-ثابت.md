# سير عمل ثابت — محلي → أونلاين

## كل يوم (تطوير واختبار)

| # | أين | ماذا |
|---|-----|------|
| 1 | جهازك | شغّل `scripts\local-dev.cmd` وجرّب على http://localhost:5173 |
| 2 | جهازك | عند الرضا: `scripts\رفع-للاونلاين.bat` → اختر **1** |
| 3 | السيرفر | `bash /var/www/erp/deploy/publish-online.sh` |
| 4 | المتصفح | **Ctrl+Shift+R** ثم افتح `/deploy-revision.txt` |

**هذا يرفع:** كل الكود (شاشات React، Laravel API، routes، migrations، i18n، CSS، ربط الواجهة بالـ API).

**لا يرفع:** ملف `.env` — ولا يمسح فواتير/عملاء الإنتاج.

---

## متى تختار النسخة الكاملة؟

فقط إذا أردت أن يصبح **موقع الإنتاج = نسخة قاعدة بياناتك المحلية بالكامل**.

| # | أين | ماذا |
|---|-----|------|
| 1 | جهازك | `scripts\رفع-كل-شيء.bat` (تصدير + push `deploy/db_backup.sql`) |
| 2 | السيرفر | `bash /var/www/erp/deploy/publish-all-online.sh` |
| 3 | المتصفح | **Ctrl+Shift+R** |

---

## أخطاء شائعة (تضيع الوقت)

| خطأ | الصواب |
|-----|--------|
| `git push` فقط | يجب تشغيل سكربت على **السيرفر** |
| تشغيل `.bat` على Linux | `.bat` = ويندوز فقط |
| لا تحديث المتصفح | **Ctrl+Shift+R** بعد كل نشر |

---

## أوامر السيرفر (احفظها)

```bash
# عادي — كود + build + migrations (+ بيانات مرجعية إن وُجدت)
bash /var/www/erp/deploy/publish-online.sh

# كامل — كود + استيراد قاعدة من deploy/db_backup.sql
bash /var/www/erp/deploy/publish-all-online.sh
```

---

## تحقق سريع

- https://firstclickerp.top/api/health → `{"ok":true}`
- https://firstclickerp.top/deploy-revision.txt → نفس آخر commit على GitHub
