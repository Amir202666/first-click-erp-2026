# Rate limiting zones — يُحمَّل داخل http {} عبر conf.d
# cp deploy/nginx/firstclick-rate-limit.conf /etc/nginx/conf.d/

limit_req_zone $binary_remote_addr zone=fc_api:10m rate=60r/m;
limit_req_zone $binary_remote_addr zone=fc_login:10m rate=5r/m;
limit_req_status 429;
